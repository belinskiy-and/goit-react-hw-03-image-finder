import styled from 'styled-components';
import {
  color,  
  layout,
  grid,
  flexbox,  
  position,
  typography,
} from 'styled-system';

const Box = styled('div')(
  color,  
  layout,
  grid,
  flexbox,  
  position,
  typography
);

export default Box;